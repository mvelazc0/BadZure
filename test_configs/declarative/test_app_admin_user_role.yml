# Declarative rewrite of test_configs/legacy/test_app_admin_user_role.yml
# Tier-1 `technique:` sugar - same chain, new shape. Live test:
#   python badzure.py build --config test_configs/declarative/test_app_admin_user_role.yml --verbose
#   python badzure.py destroy --verbose

tenant:
  tenant_id: null
  domain: null
  subscription_id: null
baseline:
  identities:
    users: 2
    groups: 1
    applications: 2
attack_paths:
  app_admin_user_role:
    technique: ApplicationAdministratorAbuse
    method: AzureADRole
    entra_role: random
