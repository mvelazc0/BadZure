# Declarative (schema: graph) rewrite of test_configs/legacy/test_app_ownership_sp_api.yml
# Tier-1 `technique:` sugar - same chain, new shape. Live test:
#   python badzure.py build --config test_configs/declarative/test_app_ownership_sp_api.yml --verbose
#   python badzure.py destroy --verbose

schema: graph
tenant:
  tenant_id: null
  domain: null
  subscription_id: null
baseline:
  identities:
    users: 1
    groups: 1
    applications: 3
attack_paths:
  app_ownership_sp_api:
    technique: ApplicationOwnershipAbuse
    initial_access: service_principal
    method: APIPermission
    api_type: graph
    app_role:
    - 06b708a9-e830-4db3-a914-8e69da51d44f
    - 9e3f62cf-ca93-4989-b6ce-bf83c28f9fe8
