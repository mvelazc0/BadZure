# Declarative rewrite of test_configs/legacy/test_mi_abuse_logicapp_sp.yml
# Tier-1 `technique:` sugar - same chain, new shape. Live test:
#   python badzure.py build --config test_configs/declarative/test_mi_abuse_logicapp_sp.yml --verbose
#   python badzure.py destroy --verbose

tenant:
  tenant_id: null
  domain: null
  subscription_id: null
baseline:
  identities:
    users: 1
    groups: 1
    applications: 3
  resources:
    storage_accounts: 1
    logic_apps: 1
attack_paths:
  mi_abuse_logicapp_sp:
    privilege_escalation: ManagedIdentityAbuse
    source_type: logic_app
    target_resource_type: storage_account
    initial_access: service_principal
    method: AzureADRole
    entra_role: 62e90394-69f5-4237-9190-012177145e10
