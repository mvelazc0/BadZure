# Declarative (schema: graph) rewrite of test_configs/legacy/test_app_admin_exchange.yml
# Tier-1 `technique:` sugar - same chain, new shape. Live test:
#   python badzure.py build --config test_configs/declarative/test_app_admin_exchange.yml --verbose
#   python badzure.py destroy --verbose

schema: graph
tenant:
  tenant_id: null
  domain: null
  subscription_id: null
baseline:
  identities:
    users: 2
    groups: 1
    applications: 2
attack_paths:
  app_admin_exchange:
    technique: ApplicationAdministratorAbuse
    method: APIPermission
    api_type: exchange
    app_role: dc890d15-9560-4a4c-9b7f-a736ec74ec40
