# Declarative rewrite of test_configs/legacy/test_app_ownership_user_role.yml
# Tier-1 `technique:` sugar - same chain, new shape. Live test:
#   python badzure.py build --config test_configs/declarative/test_app_ownership_user_role.yml --verbose
#   python badzure.py destroy --verbose

tenant:
  tenant_id: null
  domain: null
  subscription_id: null
baseline:
  identities:
    users: 2
    groups: 1
    applications: 2
attack_paths:
  app_ownership_user_role:
    privilege_escalation: ApplicationOwnershipAbuse
    initial_access: user
    method: AzureADRole
    entra_role: 62e90394-69f5-4237-9190-012177145e10
