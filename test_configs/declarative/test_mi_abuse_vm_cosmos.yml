# Declarative rewrite of test_configs/legacy/test_mi_abuse_vm_cosmos.yml
# Tier-1 `technique:` sugar - same chain, new shape. Live test:
#   python badzure.py build --config test_configs/declarative/test_mi_abuse_vm_cosmos.yml --verbose
#   python badzure.py destroy --verbose

tenant:
  tenant_id: null
  domain: null
  subscription_id: null
baseline:
  identities:
    users: 2
    groups: 1
    applications: 2
  resources:
    virtual_machines: 1
    cosmos_dbs: 1
attack_paths:
  mi_abuse_vm_cosmos:
    privilege_escalation: ManagedIdentityAbuse
    source_type: vm
    target_resource_type: cosmos_db
    method: AzureADRole
    entra_role: 62e90394-69f5-4237-9190-012177145e10
