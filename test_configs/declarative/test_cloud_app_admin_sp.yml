# Declarative (schema: graph) rewrite of test_configs/legacy/test_cloud_app_admin_sp.yml
# Tier-1 `technique:` sugar - same chain, new shape. Live test:
#   python badzure.py build --config test_configs/declarative/test_cloud_app_admin_sp.yml --verbose
#   python badzure.py destroy --verbose

schema: graph
tenant:
  tenant_id: null
  domain: null
  subscription_id: null
baseline:
  identities:
    users: 1
    groups: 1
    applications: 3
attack_paths:
  cloud_app_admin_sp:
    technique: CloudAppAdministratorAbuse
    initial_access: service_principal
    method: APIPermission
    api_type: graph
    app_role: 9e3f62cf-ca93-4989-b6ce-bf83c28f9fe8
