tenant:
  tenant_id: null
  domain: null
  subscription_id: null
attack_paths:
  declarative_unreachable:
    objective:
      name: Global Administrator via Key Vault
      capability: entra_role
      role: Global Administrator
      impact: critical
      description: Same goal as declarative_kv_theft, but the access edge is missing.
    metadata:
      complexity: low
      tags:
      - key-vault
      - negative-test
    initial_access:
      method: compromised_identity
      principal_ref: alice
    identities:
      users:
      - ref: alice
      applications:
      - ref: app_highpriv
    resources:
      resource_groups:
      - ref: rg-data
        location: West US
      key_vaults:
      - ref: kv-replace-me-k5v4k
        resource_group: rg-data
    assignments:
    - id: a2
      type: entra_role
      principal_ref: app_highpriv
      role: Global Administrator
    credentials:
    - ref: app_secret
      app_ref: app_highpriv
      type: password
    data_injects:
    - id: d1
      material: app_secret
      credential_ref: app_secret
      location: key_vault_secret
      location_ref: kv-replace-me-k5v4k
      name: client-secret-app-highpriv
uniquified: true
